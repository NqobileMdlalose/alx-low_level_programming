#include <stdio.h>

/**
 * main - prints a combination os 2 2 digit numbers
 *
 * Return: 0
 */
int main(void)
{
	int i, j, k, n;

	for ()
	{
		for (i = 48; i <= 57; i++)
		{
			putchar(i);
		
			for(j = 48; j <= 57; j++)
			{
				putchar(j);
				putchar(' ');
			}
		}
		putchar('\n');
		return (0);
	}
}
