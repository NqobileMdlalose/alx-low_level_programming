#include "main.h"

/**
 * is_palindrome - checks if the string is palindrome
 * @s: string pointer
 * @rev: integer
 * @fro: integer
 * Return: int
 */
int is_palindrome(char *s)
{
	if ()
	{
		return (1);
	}
	if (s[fro] != s[rev])
	{
		return (0);
	}
	return is_palindrome(s);
}
