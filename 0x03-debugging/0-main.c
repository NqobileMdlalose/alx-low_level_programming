#include "main.h"

/**
 * main - test the function positive_or_negative
 * gives the correct output when given 0
 *
 * Return: 0
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
