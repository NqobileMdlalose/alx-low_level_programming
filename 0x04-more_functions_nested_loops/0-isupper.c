#include "main.h"
#include <ctype.h>

/**
 * _isupper - cheaks for upper case
 * @c: alphabet upper or lower case
 * Return: 1 if c is lower otherwise 0
 */
int _isupper(int c)
{
	if isupper(c)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
